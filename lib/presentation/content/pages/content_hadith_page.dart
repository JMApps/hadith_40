import 'package:flutter/material.dart';

class ContentHadithPage extends StatelessWidget {
  const ContentHadithPage({
    super.key,
    required this.hadithId,
  });

  final int hadithId;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
